---
---

# Security & secrets

- Never commit API tokens or keys to the repo (including `.mcp.json`).
- Prefer environment variables or local-only config files for secrets.
- Do not paste `.env*` contents into chat/memory.
