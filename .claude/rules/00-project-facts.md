---
---

# Project Facts (source of truth)

- Canonical domain: `https://captureclient.com` (no `www`)
- Primary phone: `865-346-6111` (use `tel:865-346-6111`)
- Deployable app root: `capture-client-site/` (see `vercel.json`)
- Stack: Next.js 16 (App Router), React 19, TypeScript, Tailwind, Framer Motion (import motion via `@/lib/motion`)
- Package manager: `npm` (uses `package-lock.json`)

## Commands (run inside `capture-client-site/`)
- `npm run dev`
- `npm run build`
- `npm run lint`
- `npm run format`
- `npm run typecheck`
